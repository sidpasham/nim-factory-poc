apiVersion: v1
kind: Service
metadata:
  name: {{ include "model-factory.fullname" . }}
  labels:
    {{- include "model-factory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "model-factory.apiSelectorLabels" . | nindent 4 }}
