LLM GPU Benchmarking is installed.

{{- if .Values.ingress.enabled }}
Ingress access:
{{- range .Values.ingress.hosts }}
  http://{{ .host }}
{{- end }}

Service Health:
  curl -sS http://{{ (first .Values.ingress.hosts).host }}/health | jq .

Benchmark Ingest API:
  curl -sS -X POST http://{{ (first .Values.ingress.hosts).host }}/benchmarks \
    -H "Content-Type: application/json" \
    -d '{"model_name":"Qwen/Qwen2.5-0.5B-Instruct-GGUF","target_gpu":"A10G-24GB","target_environment":"kubernetes","precision_mode":"INT4"}' | jq .

Local validation warmup runs in the worker before it accepts Temporal work. With
default values, that builds llama.cpp and downloads the configured INT4 GGUF
artifact during worker startup.
{{- else }}
Ingress is disabled. Re-run the same Helm install or upgrade command with:
  --set ingress.enabled=true
  --set ingress.className=<ingress-class>
{{- end }}
