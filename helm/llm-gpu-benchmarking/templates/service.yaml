apiVersion: v1
kind: Service
metadata:
  name: {{ include "llm-gpu-benchmarking.fullname" . }}
  labels:
    {{- include "llm-gpu-benchmarking.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "llm-gpu-benchmarking.apiSelectorLabels" . | nindent 4 }}
