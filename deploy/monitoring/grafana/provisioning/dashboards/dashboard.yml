apiVersion: 1

providers:
  - name: LLM GPU Benchmarking
    orgId: 1
    folder: LLM GPU Benchmarking
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
